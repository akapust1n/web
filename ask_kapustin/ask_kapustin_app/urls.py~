from django.conf.urls import patterns, include, url

from django.contrib import admin
admin.autodiscover()

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'ask_kapustin.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),

   url(r'^$', get_post_params),
)
